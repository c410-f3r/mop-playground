image: node:8-alpine

pages:
  cache:
    paths:
    - node_modules/
  stage: deploy
  script:
    - npm install
    - npm run build
    - mv dist/* public/
  artifacts:
    paths:
    - public
  only:
  - master
