declare module "@fortawesome/*";
declare module "chart.js";

/*
declare global {
    interface GenericChart {
      datasets: {
        label: string;
        data: number[];
        borderWidth: number;
      }[];
      labels: number[];
    }
    interface Window { mop: typeof import('./mop_bindings') }
}
*/
